[up]
exe_as_root = systemctl start nginx
dep[] = nginx:installed

[installed]
exe_as_root = pacman --noconfirm -S nginx
if_not = which nginx

[down]
exe_as_root = systemctl stop nginx
dep[] = nginx:up
